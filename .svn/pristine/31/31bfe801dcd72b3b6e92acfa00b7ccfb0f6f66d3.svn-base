///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Drawing;
using System.Globalization;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace POCSmiles.Mobile.RxBrowser.DadosPassageiros
{
    public partial class InserirPassageiros
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void InsertPassengers(string index, string favorite, string name, string lastName, string genrer, string birthday, string email, string addFavorite, string numPassporte, string vencimentoPassaporte, string paisEmissor)
        {
        	try 
        	{
        		var currCultureInfo = CultureInfo.CreateSpecificCulture("pt-br");
        		if (!favorite.Equals("") || !name.Equals("") || !lastName.Equals("") || !genrer.Equals("") || !birthday.Equals("") || !email.Equals("") || !addFavorite.Equals("") || !numPassporte.Equals("") || !vencimentoPassaporte.Equals("") || !paisEmissor.Equals(""))
				{
    		    	   	if(favorite.ToUpper().Equals("TRUE"))
        		    	{
        		    	}else 
        		    	{
        		    		if (!name.Equals(""))
        		    		{
								InputTag nameField = repo.RxBrowser.Dom.Passageiros.Passageiro_NomeInfo.AbsolutePath.ToResolvedString().Replace("0",index);
								nameField.Element.SetAttributeValue("Value",name);
        		    		}
        		    		if (!lastName.Equals(""))
        		    		{
        		    			InputTag lastNameField = repo.RxBrowser.Dom.Passageiros.Passageiro_SobrenomeInfo.AbsolutePath.ToResolvedString().Replace("0",index);
        		    			lastNameField.Element.SetAttributeValue("Value",lastName);
        		    		}
        		    		if (!genrer.Equals(""))
        		    		{
        		    			LabelTag genreField = repo.RxBrowser.Dom.Passageiros.Passageiro_GeneroInfo.AbsolutePath.ToResolvedString().Replace("0",index).Replace("GENERO",genrer);
        		    			genreField.Touch();
        		    			Delay.Seconds(2);
        		    			genreField.Touch();
        		    		}
        		    		if (!birthday.Equals(""))
	        				{
        		    			string dia = System.DateTime.Parse(birthday,currCultureInfo).Day.ToString();
	        					string mes = System.DateTime.Parse(birthday,currCultureInfo).Month.ToString();
	        					string ano = System.DateTime.Parse(birthday,currCultureInfo).Year.ToString();
	        					InputTag birthdayDayField = repo.RxBrowser.Dom.Passageiros.Passageiro_DataNascimentoDiaInfo.AbsolutePath.ToResolvedString().Replace("0",index);
	        					InputTag birthdayMesField = repo.RxBrowser.Dom.Passageiros.Passageiro_DataNascimentoMesInfo.AbsolutePath.ToResolvedString().Replace("0",index);
	        					InputTag birthdayAnoField = repo.RxBrowser.Dom.Passageiros.Passageiro_DataNascimentoAnoInfo.AbsolutePath.ToResolvedString().Replace("0",index);
	        					birthdayDayField.Element.SetAttributeValue("Value",dia);
	        					birthdayMesField.Element.SetAttributeValue("Value",mes);
	        					birthdayAnoField.Element.SetAttributeValue("Value",ano);
	        				}
        		    		if (!email.Equals(""))
        		    		{
        		    			InputTag emailField = repo.RxBrowser.Dom.Passageiros.Passageiro_EmailInfo.AbsolutePath.ToResolvedString().Replace("0",index);	
        		    			emailField.Element.SetAttributeValue("Value",email);
        		    		}
        		    		if (addFavorite.ToUpper().Equals("TRUE"))
        		    		{
        		    			InputTag fav = repo.RxBrowser.Dom.Passageiros.Passageiro_FavoritoInfo.AbsolutePath.ToResolvedString().Replace("0",index);
        		    			fav.Touch();
        		    		}
        		    	}
    		    	if (!numPassporte.Equals("")) {
						InputTag passportField = repo.RxBrowser.Dom.Passageiros.Passageiro_PassaporteInfo.AbsolutePath.ToResolvedString().Replace("0",index);
        		    	passportField.Element.SetAttributeValue("Value",numPassporte);	        		    			
    		    	}
        		    if (!vencimentoPassaporte.Equals("")) {
        		    	string dia = System.DateTime.Parse(vencimentoPassaporte, currCultureInfo).Day.ToString();
	        			string mes = System.DateTime.Parse(vencimentoPassaporte,currCultureInfo).Month.ToString();
	        			string ano = System.DateTime.Parse(vencimentoPassaporte,currCultureInfo).Year.ToString();
	        			InputTag validDayField = repo.RxBrowser.Dom.Passageiros.Passageiro_DataVencimentoPassaporteDiaInfo.AbsolutePath.ToResolvedString().Replace("0",index);
	        			InputTag validMesField = repo.RxBrowser.Dom.Passageiros.Passageiro_DataVencimentoPassaporteMesInfo.AbsolutePath.ToResolvedString().Replace("0",index);
	        			InputTag validAnoField = repo.RxBrowser.Dom.Passageiros.Passageiro_DataVencimentoPassaporteAnoInfo.AbsolutePath.ToResolvedString().Replace("0",index);
	        			validDayField.Element.SetAttributeValue("Value",dia);
	        			validMesField.Element.SetAttributeValue("Value",mes);
	        			validAnoField.Element.SetAttributeValue("Value",ano);
        		    }
        		    if (!paisEmissor.Equals("")) {
        		    	Utils.SelectItemListBox(repo.RxBrowser.Dom.Passageiros.Passageiro_PaisEmissorInfo.AbsolutePath.ToResolvedString().Replace("0", index),paisEmissor);
        		    }
        		}
        	} 
        	catch (Exception e)
        	{
        		Report.Error("InsertPassengers - Error inserting passengers. Error: " + e.Message);
        		throw new System.Exception("Fatal error");
        	}
        }

    }
}
