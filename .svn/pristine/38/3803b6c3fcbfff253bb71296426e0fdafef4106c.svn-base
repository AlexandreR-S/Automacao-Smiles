///////////////////////////////////////////////////////////////////////////////
//
// FormProgesso file was automatically generated by RANOREX.
// Your custom recording code should go in FormProgesso file.
// The designer will only add methods to FormProgesso file, so your custom code won't be overwritten.
// http://www.ranorex.com
// 
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Drawing;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace POCSmiles.Base_Dados
{
    public partial class RandomValues
    {
        /// <summary>
        /// FormProgesso method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void ReplaceRandomValues(string url, string CPFSmilesID, string senha, string de1, string para1, string de2, string tipoDestino, string para2)
        {
        	try {
        		int temp;
        		if (CPFSmilesID.Equals("PERFIL_ALEATORIO")) {
        			//Getting Database URL from global parameters
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
					_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
					_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_SALDO_DE_COMPRA_DE_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_COMPRA < " + Utils.getGlobalValue("MAX_COMPRA_MILHAS") + " AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_NÃO_CLUBE_COM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_MILHAS > " + Utils.getGlobalValue("MIN_MILHAS") + " AND CLUBE = FALSE AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CLUBE_SMILES_COM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_MILHAS > " + Utils.getGlobalValue("MIN_MILHAS") + " AND CLUBE = TRUE AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_COM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_MILHAS > " + Utils.getGlobalValue("MIN_MILHAS") + " AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_SEM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_MILHAS < " + Utils.getGlobalValue("MIN_MILHAS") + " AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CLUBE_SMILES_SEM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_MILHAS < " + Utils.getGlobalValue("MIN_MILHAS") + " AND CLUBE = TRUE AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_NAO_CLUBE")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE CLUBE = FALSE AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_NAO_CLUBE_SEM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE SALDO_MILHAS < " + Utils.getGlobalValue("MIN_MILHAS") + " AND CLUBE = FALSE AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_VOO_EMITIDO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = FALSE AND B.VOO_RESERVADO = FALSE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF = " + _CPFSmilesID + " AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = FALSE AND B.VOO_RESERVADO = FALSE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_ASSENTO_SELECIONADO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = TRUE AND B.VOO_CANCELADO = FALSE AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON B.CPF = A.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF = " + _CPFSmilesID + " AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = TRUE AND B.VOO_CANCELADO = FALSE AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_VOO_RESERVADO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_RESERVADO = TRUE AND B.VOO_CANCELADO = FALSE AND DateDiff(\"d\",B.DATA_MODIFICACAO,Date()) <= 3 AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF = " + _CPFSmilesID + " AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_RESERVADO = TRUE AND B.VOO_CANCELADO = FALSE AND DateDiff(\"d\",B.DATA_MODIFICACAO,Date()) <= 3 AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_VOO_RESERVADO_G3")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = FALSE AND B.VOO_RESERVADO = TRUE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]' OR VOO_2 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = AB.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF = " + _CPFSmilesID + " AND B.DATA_PARTIDA > DATE() AND B.ASSENTO_SELECIONADO = FALSE AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_RESERVADO = TRUE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]' OR VOO_2 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_VOO_EMITIDO_G3")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = FALSE AND B.VOO_RESERVADO = FALSE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF = " + _CPFSmilesID + " AND B.DATA_PARTIDA > DATE() AND B.ASSENTO_SELECIONADO = FALSE AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_RESERVADO = FALSE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_VOO_EMITIDO_YY")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.ASSENTO_SELECIONADO = FALSE AND B.VOO_RESERVADO = FALSE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF = " + _CPFSmilesID + " AND B.DATA_PARTIDA > DATE() AND B.ASSENTO_SELECIONADO = FALSE AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_RESERVADO = FALSE AND B.VOO_CANCELADO = FALSE AND B.DATA_MODIFICACAO <= DATE() AND (VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_RESERVA_ISENTO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CLUBE = TRUE AND CPF <> 0 AND NUM_RESERVAS <= " + Utils.getGlobalValue("MAX_RESERVAS_ISENTO"));
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_RESERVA_NAO_ISENTO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND SALDO_MILHAS > " + Utils.getGlobalValue("MIN_MILHAS") + " AND (CLUBE = FALSE OR NUM_RESERVAS > " + Utils.getGlobalValue("MAX_RESERVAS_ISENTO") + ") AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAMENTO_COM_PAGAMENTO_G3")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE()  AND A.CPF <> 0 AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (TARIFAS_POR_ADULTO_DINHEIRO * QNT_PASSAGEIROS) < " + Utils.getGlobalValue("MAX_VALOR_REEMBOLSO"));
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE A.CPF = " + _CPFSmilesID + " AND B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (TARIFAS_POR_ADULTO_DINHEIRO * QNT_PASSAGEIROS) < " + Utils.getGlobalValue("MAX_VALOR_REEMBOLSO") + " AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAMENTO_REEMBOLSO_G3")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF <> 0 AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (TARIFAS_POR_ADULTO_DINHEIRO * QNT_PASSAGEIROS) > " + Utils.getGlobalValue("MAX_VALOR_REEMBOLSO"));
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE A.CPF = " + _CPFSmilesID + " AND B.NUM_RESERVA <> '' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (TARIFAS_POR_ADULTO_DINHEIRO * QNT_PASSAGEIROS) > " + Utils.getGlobalValue("MAX_VALOR_REEMBOLSO") + " AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CLUBE")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE CLUBE = TRUE AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_EMISSAO_SEM_MILHAS")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA LIKE 'SEM_MILHAS%'  AND A.CPF <> 0 AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA LIKE 'SEM_MILHAS%' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE AND A.CPF = " + _CPFSmilesID + " AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CRIAR_EVENTO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT CPF FROM SMILES WHERE CPF NOT IN (SELECT CPF FROM GIFT WHERE FINALIZADO = TRUE) AND URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAR_EVENTO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN GIFT AS B ON A.CPF = B.CPF WHERE B.FINALIZADO = FALSE AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_FAVORITO_CADASTRADO")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN FAVORITOS AS B ON A.CPF = B.CPF WHERE A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			NumeroDocumentoGrid = UtilsDB.getRandomFavorite("SELECT * FROM SMILES AS A INNER JOIN FAVORITOS AS B ON A.CPF = B.CPF WHERE A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND A.CPF = " + _CPFSmilesID);
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAMENTO_COM_PAGAMENTO_YY")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.NUM_RESERVA NOT LIKE '#SEM_MILHAS#' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND A.CPF <> 0");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE A.CPF = " + _CPFSmilesID + " AND B.NUM_RESERVA <> '' AND B.NUM_RESERVA NOT LIKE '#SEM_MILHAS#' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAMENTO_REEMBOLSO_YY")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.NUM_RESERVA NOT LIKE '#SEM_MILHAS#'  AND A.CPF <> 0 AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (TARIFAS_POR_ADULTO_DINHEIRO * QNT_PASSAGEIROS) > " + Utils.getGlobalValue("MAX_VALOR_REEMBOLSO"));
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE A.CPF = " + _CPFSmilesID + " AND B.NUM_RESERVA <> '' AND B.NUM_RESERVA NOT LIKE '#SEM_MILHAS#' AND B.DATA_PARTIDA > DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND (VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]') AND (TARIFAS_POR_ADULTO_DINHEIRO * QNT_PASSAGEIROS) > " + Utils.getGlobalValue("MAX_VALOR_REEMBOLSO") + " AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAMENTO_MESMO_DIA_G3")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND A.CPF <> 0 AND B.DATA_MODIFICACAO = DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]'");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE A.CPF = " + _CPFSmilesID + " AND B.NUM_RESERVA <> '' AND B.DATA_MODIFICACAO = DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND VOO_1 LIKE 'G3 - [0-9][0-9][0-9][0-9]' AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(CPFSmilesID.Equals("PERFIL_CANCELAMENTO_MESMO_DIA_YY")){
        			_CPFSmilesID = UtilsDB.getRandomUser("SELECT A.CPF FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE B.NUM_RESERVA <> '' AND B.NUM_RESERVA NOT LIKE '#SEM_MILHAS#'  AND A.CPF <> 0 AND B.DATA_MODIFICACAO = DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]'");
        			_Senha = UtilsDB.getDataValue(_CPFSmilesID, url, "SENHA");
        			_IdentificacaoVoo = UtilsDB.getRandomFly("SELECT B.NUM_RESERVA FROM SMILES AS A INNER JOIN PNR AS B ON A.CPF = B.CPF WHERE A.CPF = " + _CPFSmilesID + " AND B.NUM_RESERVA <> '' AND B.NUM_RESERVA NOT LIKE '#SEM_MILHAS#' AND B.DATA_MODIFICACAO = DATE() AND A.URL LIKE '" + (url.Contains("portal")?"%portal%":url) + "' AND B.VOO_CANCELADO = FALSE AND B.VOO_RESERVADO = FALSE  AND VOO_1 NOT LIKE 'G3 - [0-9][0-9][0-9][0-9]' AND (B.NUM_RESERVA NOT LIKE 'SEM_MILHAS%' AND B.NUM_RESERVA NOT LIKE 'PEDIDO_%')");
        			_TecladoVirtual = _Senha;
        		}
        		else if(int.TryParse(CPFSmilesID, out temp) && CPFSmilesID.Length == 11){
        			if (!UtilsDB.checkExist("SELECT CPF FROM SMILES WHERE CPF = " + CPFSmilesID)) {
        				UtilsDB.insertUser("00000000000", senha, url);
        			}
        		}
        		if (de1.Equals("ALEATORIO")) {
        			if (tipoDestino.Equals("G3")) {
        				de1 = UtilsDB.getRandomDestiny("TIPO LIKE 'G3'");
        				De1 = de1;
	        		}
        			else if(tipoDestino.Equals("YY")){
        				de1 = UtilsDB.getRandomDestiny("TIPO LIKE 'G3'");
        				De1 = de1;
	        		}
        		}
        		if (de2.Equals("ALEATORIO")) {
        			if (tipoDestino.Equals("G3")) {
        				de2 = UtilsDB.getRandomDestiny("TIPO LIKE 'G3' AND DESTINO NOT LIKE '" + de1 + "' AND DESTINO NOT LIKE '" + para1 + "'");
        				De2 = de2;
	        		}
        			else if(tipoDestino.Equals("YY")){
        				de2 = UtilsDB.getRandomDestiny("TIPO LIKE 'YY' AND DESTINO NOT LIKE '" + de1 + "' AND DESTINO NOT LIKE '" + para1 + "'");
        				De2 = de2;
	        		}
        		}
        		if (para1.Equals("ALEATORIO")) {
        			if (tipoDestino.Equals("G3")) {
        				para1 = UtilsDB.getRandomDestiny("TIPO LIKE 'G3' AND DESTINO NOT LIKE '" + de1 + "'");
        				Para1 = para1;
	        		}
        			else if(tipoDestino.Equals("YY")){
        				para1 = UtilsDB.getRandomDestiny("TIPO LIKE 'YY' AND DESTINO NOT LIKE '" + de1 + "'");
        				Para1 = para1;
	        		}
        		}
        		if (para2.Equals("ALEATORIO")) {
        			if (tipoDestino.Equals("G3")) {
        				para2 = UtilsDB.getRandomDestiny("TIPO LIKE 'G3' AND DESTINO NOT LIKE '" + de1 + "' AND DESTINO NOT LIKE '" + de1 + "' AND DESTINO NOT LIKE '" + de2 + "'");
        				Para2 = para2;
	        		}
        			else if(tipoDestino.Equals("YY")){
        				para2 = UtilsDB.getRandomDestiny("TIPO LIKE 'YY' AND DESTINO NOT LIKE '" + de1 + "' AND DESTINO NOT LIKE '" + de1 + "' AND DESTINO NOT LIKE '" + de2 + "'");
        				Para2 = para2;
	        		}
        		}
        	} catch (Exception e) {
        		Report.Failure("Database query execution  exception - Error replacing random values or there is any value for selected data: " + e.Message);	
        	}
        }
    }
}