///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
// 
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Drawing;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace POCSmiles.GIFT
{
    public partial class BuscarEvento
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        private void TypeKeys(string element, string text, string URL){
        	try 
        	{
        		if (text.Equals("")) {
        			return;
        		}
        		if (text.ToUpper().Equals("ALEATORIO")) {
        			text = Base_Dados.UtilsDB.getRandomGIFT("SELECT NOME FROM SMILES AS A INNER JOIN GIFT AS B ON A.CPF = B.CPF WHERE B.FINALIZADO = FALSE AND A.URL LIKE '" + (URL.Contains("portal")?"%portal%":URL) + "'");
        		}
        		if (text.ToUpper().Equals("ALEATORIO")) {
        			text = "";
        		}
        		Report.Log(ReportLevel.Info,"Inserting text '" + text + "'");
        		WebElement input = repo.SmilesOMelhorProgramaDeMilhasS1.AbsoluteBasePath.ToResolvedString() + element;
        		input.PressKeys(text,Duration.FromMilliseconds(1));
        		Report.Log(ReportLevel.Info,"Text inserted '" + text + "'");
        	} catch (Exception e) 
        	{
        		Report.Error("BuscarEvento - Error launched trying to inserting text '" + text + "' on element '" + element + "'.Error: " + e.Message);
        		throw new System.Exception("BuscarEvento.TypeKeys");
        	}
        }

        public void WaitSystemLoad(string timeout)
        {
        	try {
        		Report.Info("Wainting system load");
        		Smiles_UserCode.WaitLoad(repo, int.Parse(timeout));
        		Report.Info("System loaded");
        	} catch (Exception) {
        		Report.Error("BuscarVoos - Error waiting system load. Timeout:" + timeout);
        		throw new System.Exception("BuscarVoos.WaitSystemLoad");
        	}
        }

        public void WaitEvent()
        {
            // TODO: Replace the following line with your code implementation.
            throw new NotImplementedException();
        }

    }
}